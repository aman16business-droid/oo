{
  "entities": {
    "SiteSettings": {
      "title": "Site Settings",
      "description": "Configuration and assets for the website",
      "type": "object",
      "properties": {
        "heroBanner": {
          "type": "string",
          "description": "URL of the hero banner image"
        },
        "menBanner": {
          "type": "string",
          "description": "URL of the men's category banner image"
        },
        "womenBanner": {
          "type": "string",
          "description": "URL of the women's category banner image"
        }
      }
    }
  },
  "firestore": {
    "/settings/siteSettings": {
      "schema": "SiteSettings",
      "description": "Global site configuration document"
    }
  }
}
